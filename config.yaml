start_date: "2026-07-01"
end_date: "2026-08-13"
minimum_tournament_players: 60
game: PTCG
format: STANDARD
platform: PTCGL
online_only: true
decklists_required: true
match_scope: all
request_timeout_seconds: 30
request_retries: 5
minimum_request_interval_seconds: 0.20
incomplete_cache_ttl_minutes: 15
incomplete_refresh_days: 3
discovery_cache_ttl_minutes: 15
data_dir: data

# Raw API responses are retained. Entries and matches from these tournament IDs
# are omitted only when analytics are rebuilt.
excluded_tournaments: {}
# Example:
# excluded_tournaments:
#   6a0000000000000000000000: "Special rules / not the target Standard pool"

# Optional future mapping layer. It is deliberately empty and disabled in MVP.
archetype_groups: {}
